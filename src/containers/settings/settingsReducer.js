const initionState = {
  setting: null,
};

const settingsReducer = (state = initionState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default settingsReducer;
