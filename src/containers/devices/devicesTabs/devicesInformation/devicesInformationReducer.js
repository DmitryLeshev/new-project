// import {} from "./actionsTypes";

const initionState = {
  devicesInformation: null,
};

const devicesInformationReducer = (state = initionState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default devicesInformationReducer;
